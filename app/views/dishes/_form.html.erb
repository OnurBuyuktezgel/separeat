<%= simple_form_for [@restaurant, @dish] do |f| %>
  <%= f.input :category, collection: Dish::CATEGORIES %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price, label: 'Price (€)' %>
  <%= f.input :todays_special, label: 'Today\'s Special' %>
<!--   <%= f.input :photo, label: 'Add a photo for your dish', as: :file %> -->
  <%= f.button :submit, 'Create Dish', class: 'btn-flat' %>
<% end %>
